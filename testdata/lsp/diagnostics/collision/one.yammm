// Test fixture for type collision diagnostics (file 1 of 2).
// Expected: E_TYPE_COLLISION error when both one.yammm and two.yammm
// are in the same import closure and both define "Product".
schema "collision_one"

import "./two"

// This type collides with Product in two.yammm
type Product {
	sku String primary
	name String
	price Integer
}

type Order {
	orderId String primary
	--> PRODUCT (one) Product
}
